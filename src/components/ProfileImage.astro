<script>
    function loadScript(src: string) {
        var script = document.createElement("script");
        script.src = src;
        script.type = "module";
        document.head.appendChild(script);
    }

    if (window.innerWidth >= 768) {
        loadScript("/src/scripts/ThreeJsObject.js");
    }
</script>
